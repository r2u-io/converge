import styled from 'styled-components'

export const Button = styled.button`
  grid-column: 1 / -1;
  grid-row: 3 / 4;
  z-index: 0;

  justify-self: end;
  margin: 10px;

  pointer-events: all;
  cursor: pointer;

  padding: 0px;
  border: none;
  outline: none;
  background: none;
  display: flex;
  justify-content: center;
  align-items: center;

  color: #54439b;
`
