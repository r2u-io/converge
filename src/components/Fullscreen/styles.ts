import styled from 'styled-components'

export const Button = styled.button`
  position: absolute;
  bottom: 10px;
  right: 10px;

  pointer-events: all;
  cursor: pointer;

  padding: 0px;
  border: none;
  outline: none;
  background: none;
  display: flex;
  justify-content: center;
  align-items: center;

  color: #54439b;
`
